@model MVC_Security.Models.ApplicationUser

@{
    ViewBag.Title = "Verwijderen";
}

<h2>User @Model.UserName Verwijderen?</h2>
 @using(Html.BeginForm("VerwijderUserDoorvoeren", "User", new { id = Model.Id }))
 {
     <input type="submit" value="Delete" />
 }

